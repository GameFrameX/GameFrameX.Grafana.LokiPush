# Docker环境变量配置示例
# 复制此文件为 .env 并修改相应的值

# 数据库连接字符串
DATABASE_CONNECTION_STRING=Host=localhost;Port=5432;Database=loki_logs;Username=postgres;Password=your_password

# 批处理配置
BATCH_SIZE=100
FLUSH_INTERVAL_SECONDS=30
MAX_QUEUE_SIZE=10000

# ASP.NET Core 配置
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:5000

# 日志级别
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft.AspNetCore=Warning